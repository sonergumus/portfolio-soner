import React from 'react';
import ReactDOM from 'react-dom/client';
import HeroEpic from './HeroEpic.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HeroEpic />
  </React.StrictMode>,
);
